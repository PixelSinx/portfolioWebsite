<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Twig flashing</title>
    </head>
    <body>
        {% if flash.getMessage('global').0 %}
            <div class="alert alert-success">
                {{ flash.getMessage('global').0 }}
            </div>
        {% endif %}

        <div class="container">
            {% block content %}{% endblock %}
        </div>

    </body>
</html>
